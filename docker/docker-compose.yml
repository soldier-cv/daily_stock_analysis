# ===================================
# A股自选股智能分析系统 - Docker Compose
# ===================================

name: daily-stock-analysis

x-common: &common
  build:
    context: ..
    dockerfile: docker/Dockerfile
  restart: unless-stopped

  env_file:
    - ../.env

  volumes:
    - /data/daily_stock_analysis/data:/app/data
    - /data/daily_stock_analysis/logs:/app/logs
    - /data/daily_stock_analysis/reports:/app/reports
    - /data/daily_stock_analysis/strategies:/app/strategies:ro
    # Longbridge OAuth token cache persistence; entrypoint repairs container-side ownership for dsa user.
    # Create /data/daily_stock_analysis/longbridge_tokens on the host before copying an authorized token cache into it.
    - /data/daily_stock_analysis/longbridge_tokens:/home/dsa/.longbridge
    # 如需覆盖前端静态资源，可挂载本地 static 目录
    # - ../static:/app/static:ro

  environment:
    - TZ=Asia/Shanghai
    - WEBUI_HOST=0.0.0.0

  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"
  deploy:
    resources:
      limits:
        memory: 512M
      reservations:
        memory: 256M

services:
  # 定时任务模式
  analyzer:
    <<: *common
    container_name: stock-analyzer

  # FastAPI 模式
  server:
    <<: *common
    container_name: stock-server
    command: ["python", "main.py", "--serve-only", "--host", "0.0.0.0", "--port", "${API_PORT:-8000}"]
    ports:
      - "5090:${API_PORT:-8000}"
